var express = require('express');
var request = require('supertest');
var slydpay_mantis = require('../index');
// import slydpay_mantis from '../index';

var app = express();

let data = {
    payment_method: 'xxxxx',//Service provider
    payment_type: 'xxxx',//payment type, optional
    amount: 'xxxx', //amount paying
    product_id: '',// product id
    payment_desc: 'xxxxx',//payment description
    customerName: 'xxxxx',//customer name,
    phone_number: 'xxxxxxxx',//customer phone,
    values: {
        emailOrMobileNumber: 'xxxx', //slydpay email
        merchantKey: 'xxxxx', //slydpay merchant key
    },
    callback_url: 'http://xxxx'// url to send payment info to when process is completed
}

describe('Function Test', () => {
    it('resolves', async (done) => {
        await slydpay_mantis.slydpay_invoice(data, (err, results) => {
            if (err) return err.message;
            return results
        });
    });
});